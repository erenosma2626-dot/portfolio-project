import { en } from "./en";

/**
 * Aktif içerik. TR/EN geçişi eklendiğinde burada locale'e göre seçilecek;
 * bileşenler yalnızca `content`'i okur.
 */
export const content = en;
export type { Content, Experience, Project } from "./types";
